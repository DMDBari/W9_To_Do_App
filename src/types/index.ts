
export type TaskType = {
    title:string,
    description:string,
    dueDate:string,
    complete:boolean,
    dateCreated:string,
}

export type TaskFormDataType = {
    title:string,
    description:string,
    dueDate:string,
}